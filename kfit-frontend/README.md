# KFit 前端

KFit是一个个人健康分析系统的现代化前端，基于Streamlit和FastAPI构建。

## 特性

- 📊 实时健康数据仪表盘
- 📈 交互式数据可视化
- ⚙️ 图形化配置管理
- 📝 健康报告查看和导出
- 📤 数据导入和管理

## 技术栈

- **前端**: Streamlit + Plotly + Altair
- **后端**: FastAPI + Python
- **数据**: Pandas + NumPy
- **部署**: Docker

## 快速开始

### 本地开发

1. 安装依赖:
```bash
cd kfit-frontend
pip install -r requirements.txt
```

2. 启动后端:
```bash
uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
```

3. 启动前端:
```bash
streamlit run app/main.py --server.port 8501 --server.address 0.0.0.0
```

4. 访问前端:
打开浏览器访问 http://localhost:8501

### Docker部署

1. 构建镜像:
```bash
docker build -t kfit-frontend .
```

2. 运行容器:
```bash
docker run -p 8000:8000 -p 8501:8501 kfit-frontend
```

## 项目结构

```
kfit-frontend/
├── app/                    # 前端主应用
├── components/             # 可复用组件
├── services/               # 业务逻辑层
├── api/                    # FastAPI后端
├── static/                 # 静态资源
├── tests/                  # 测试
├── requirements.txt        # 依赖
└── Dockerfile              # 容器化配置
```

## 开发计划

- ✅ 项目结构搭建
- 🚧 基础仪表盘实现
- ⏳ API接口开发
- ⏳ 数据可视化组件
- ⏳ 配置管理功能

## 贡献

欢迎提交问题和功能请求！

## 许可证

MIT License